import json;

use json::{ from_str, Deserialize };

use std::io::{ print };

#[derive(Deserialize)]
struct H {
    name: str;
}

fun main(): unit {
    let js = r"""{ "name": "Alice" }""";

    let h = from_str<H>(js);

    print("{}", h);
}